# Notes: <slug>

## What was built

<!-- Summary of the implementation, mapped back to the spec's ACs. -->

## How to verify

<!-- Exact commands/steps a reviewer runs to confirm the work. -->

## TDD

<!-- Required when [defaults].tdd = true (the default). Two trimmed runs:
     RED (failing tests per AC, before implementation) and GREEN (passing,
     after). Tests are never weakened to pass — the reviewer checks this. -->

### RED

```
<failing test run>
```

### GREEN

```
<passing test run>
```
