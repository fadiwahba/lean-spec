description = "Rebuild lean-spec workflow state after pause, compaction, or model change."

prompt = """
Follow the lean-spec manual workflow from GEMINI.md and .gemini/LEAN_SPEC_INSTRUCTIONS.md.

Resume lean-spec state for feature slug: {{args}}

Requirements:
- read `spec.md`, `review.md`, and `notes.md` in that order
- prefer the Gemini Pro session for resume work
- rebuild state from canonical feature artifacts only
- do not make code changes before state is rebuilt
- stop after the report unless the human explicitly asked to proceed
"""
