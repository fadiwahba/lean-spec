description = "Print current phase, last activity, and the next command for one or all features"

prompt = """
Arguments: `{{args}}`  (optional — a slug, or empty for all features)

!{
python3 $HOME/.gemini/extensions/lean-spec/bin/spec_status.py "{{args}}"
}
"""
