description = "Print the next phase command for a feature (no auto-driver in Gemini — SlashCommand dispatch unavailable)"

prompt = """
Arguments: `{{args}}`  (slug, optionally --unattended — flag is accepted but ignored in degraded mode)

!{
python3 $HOME/.gemini/extensions/lean-spec/bin/auto.py "{{args}}"
}
"""
