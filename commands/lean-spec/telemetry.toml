description = "Print per-feature phase durations from the opt-in telemetry log (read-only in Gemini — no Stop-hook sync)"

prompt = """
Arguments: `{{args}}`  (optional slug to filter; `--project <name>` to filter by project basename; empty = all)

!{
python3 $HOME/.gemini/extensions/lean-spec/bin/telemetry.py "{{args}}"
}
"""
